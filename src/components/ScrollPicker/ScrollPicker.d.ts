import { ScrollPickerProps } from '../../interfaces';
declare const ScrollPicker: ({ items, selectedIndex, setSelectedIndex, }: ScrollPickerProps) => import("react/jsx-runtime").JSX.Element;
export default ScrollPicker;
