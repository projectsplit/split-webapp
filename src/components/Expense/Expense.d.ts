import { ExpenseProps } from '../../interfaces';
declare const Expense: ({ timeZoneId, onClick, onLongPress, amount, currency, description, location, occurred, userAmount, labels, mode, }: ExpenseProps) => import("react/jsx-runtime").JSX.Element;
export default Expense;
