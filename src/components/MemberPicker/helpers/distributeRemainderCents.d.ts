import { PickerMember } from '../../../types';
export declare function distributeRemainderCents(decimalDigits: number, totalAmount: number, screenArray: number[], synchronizedFormMembers: PickerMember[]): {
    adjustedUnlockedToOriginalAmount: string[];
    adjustedLockedToOriginalAmount: string[];
};
