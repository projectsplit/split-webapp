import { SearchTransactionsProps } from '../../interfaces';
export default function SearchTransactions({ menu, group, userInfo, timeZoneId, expenseParsedFilters, transferParsedFilters, isPersonal, }: SearchTransactionsProps): import("react/jsx-runtime").JSX.Element;
