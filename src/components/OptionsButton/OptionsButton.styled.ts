import { styled } from "styled-components";

export const StyledOptionsButton = styled.div`
  border-radius: 8px;
  padding-top: 0.5rem;
  padding-bottom: 0.5rem;
  padding-left: 0.8rem;
  padding-right: 0.8rem;
  cursor: pointer;
  font-weight: bold;
  text-decoration: none;
`;
