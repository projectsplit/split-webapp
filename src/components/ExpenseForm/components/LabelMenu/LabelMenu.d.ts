import { LabelMenuProps } from '../../../../interfaces';
export declare const LabelMenu: ({ labelMenuIsOpen, groupId, labels, setLabels, userId, isPersonal, }: LabelMenuProps) => import("react/jsx-runtime").JSX.Element;
export default name;
