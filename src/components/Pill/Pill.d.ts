import { PillProps } from '../../interfaces';
export default function Pill({ title, color, closeButton, onClick, onClose, fontSize, $textColor, $border, $closeButtonColor, children, }: PillProps & {
    children?: React.ReactNode;
}): import("react/jsx-runtime").JSX.Element;
