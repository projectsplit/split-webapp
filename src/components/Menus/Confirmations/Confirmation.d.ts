import { ConfirmationProps } from '../../../interfaces';
export default function InfoContainer({ children, isLoading, onClick, menu, header, }: ConfirmationProps): import("react/jsx-runtime").JSX.Element;
