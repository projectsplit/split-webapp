import { Frequency } from '../../../types';
import { Signal } from '@preact/signals-react';
export declare const useCycleIndexEffect: (selectedCycle: Signal<Frequency>, selectedTimeCycleIndex: Signal<number>, currentWeekIndex: number, selectedYear: number) => void;
