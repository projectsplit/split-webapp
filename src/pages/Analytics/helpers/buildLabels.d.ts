import { Frequency } from '../../../types';
export declare const buildLabels: (cycle: Frequency, selectedTimeCycleIndex: number, datesToNumbers: number[], monthsAndDaysArrays: string[][], fractalFactor: number) => string[];
