import { Frequency } from '../../../types';
export declare const isCurrentPeriod: (cycle: Frequency, selectedTimeCycleIndex: number, isSuccess: boolean, cumulArrayData: number[], currentWeekIndex: number, selectedYear: number) => boolean;
