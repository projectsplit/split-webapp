import { Frequency } from '../../../../../types';
export declare const getChartOptions: (isSuccess: boolean, monthsAndDaysArrays: string[][], selectedCycle: Frequency, labels: string[], selectedYear: number, selectedTimeCycleIndex: number, currency: string) => any;
